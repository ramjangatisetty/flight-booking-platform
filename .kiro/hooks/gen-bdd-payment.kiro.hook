{
  "name": "Generate BDD Tests - Payment",
  "version": "1.0.0",
  "description": "Generate or update Cucumber feature files and step definitions for Payment Service. Manually triggered.",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Follow .kiro/agents/gen-bdd-tests.md with SERVICE=payment-service MODE=delta to ensure step definitions exist for all scenarios in the payment feature files. Only add missing step definitions - do not modify existing ones."
  }
}
