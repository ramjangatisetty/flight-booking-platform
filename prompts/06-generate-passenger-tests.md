# Prompt 06 — Generate Passenger Service Tests

Follow `TEST_GENERATION_BLUEPRINT.md`.

Create PassengerApi + builders + validator and generate:
- create → get → patch/update → delete
- correlation test
- negative tests (invalid json, not found)
  Output diffs only.
